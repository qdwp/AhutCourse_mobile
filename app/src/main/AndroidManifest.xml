<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lesson.myahut"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="23" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_LOGS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CALL_LOG" />

    <!-- <android:uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ahutlesson"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.PreScoreActivity"
            android:label="@string/score_title"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".activity.PreTaskActivity"
            android:label="@string/task_title"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".activity.PreMakeUpActivity"
            android:label="@string/makeup_title"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".activity.PreAvgPointActivity"
            android:label="@string/avgpoint_title"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".activity.ViewScoreActivity"
            android:label="@string/score_view_title"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".activity.ViewTaskActivity"
            android:label="@string/task_view_title"
            android:windowSoftInputMode="stateHidden" />
        <activity
            android:name=".activity.ViewMakeUpActivity"
            android:label="@string/makeup_view_title"
            android:windowSoftInputMode="stateHidden" />
    </application>

</manifest>
